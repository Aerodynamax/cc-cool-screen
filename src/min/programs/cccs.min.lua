require(settings.get("ghu.base").."core/apis/ghu")if fs.exists("dev_env.lua")then require("dev_env")end;require("sc.reduce")local a=require("sc.screen")a.reset()a.clear()a.setTextScale(5)a.writeCenter("Welcome!",colors.white,colors.blue," ")a.flush()a.reset()a.setCursorPos(1,2)local b=require("sc.storage")local c=b.totalCount()a.write("storage: "..c)
