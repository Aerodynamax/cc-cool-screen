require(settings.get("ghu.base").."core/apis/ghu")if fs.exists("dev_env.lua")then require("dev_env")end;print("starting up ...")local a=require("sc.screen")a.reset()a.clear()a.writeCenter("screen",colors.white,colors.blue," ")local b=require("sc.storage")if b==nil then a.setTextScale(3)a.writeCenter("ERROR: no storage found",colors.red)end;a.setCursorPos(1,3)a.writeColor("indexing storage ...",colors.white,colors.black)a.setCursorPos(1,4)a.writeColor(" (this may take some time ...)",colors.gray,colors.black)a.flush()a.reset()sleep(3)if b then parallel.waitForAll(b.index.index,function()while not b.index.isIndexed do sleep(0)a.setCursorPos(1,3)a.clearLine()a.write(b.index.latestLog[#b.index.latestLog])end end)end;a.setCursorPos(1,4)a.clearLine()a.flush()a.reset()print("started!")while true do a.writeRight(""..os.date("%a %H:%M:%S"),1,colors.white,colors.blue)a.reset()a.setCursorPos(1,3)if b then a.write("storage: "..b.count().."/"..b.maxCount().." ("..b.percentage().."%)")end;sleep(0)end
