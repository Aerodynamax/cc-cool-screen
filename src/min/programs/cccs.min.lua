require(settings.get("ghu.base").."core/apis/ghu")if fs.exists("dev_env.lua")then require("dev_env")end;print("starting up ...")local a=require("sc.screen")a.reset()a.clear()a.writeCenter("screen",colors.white,colors.blue," ")a.flush()a.reset()print("started!")local b=require("sc.storage")while true do a.writeRight(""..os.date("%a %H:%M:%S"),1,colors.white,colors.blue)a.reset()a.setCursorPos(1,3)a.write("storage: "..b.totalCount())end
